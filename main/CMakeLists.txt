idf_component_register(SRCS ./main.c ./nvs.c
                       INCLUDE_DIRS .                       # Edit following two lines to set component requirements (see docs)
                       REQUIRES
                       PRIV_REQUIRES )

